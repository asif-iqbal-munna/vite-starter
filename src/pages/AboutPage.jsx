function AboutPage() {
  return (
    <main className="page">
      <section className="page">
        <h1 className="page-title">About this starter</h1>
        <p className="page-description">
          This page is styled with Tailwind CSS using the @apply directive, so
          you can keep a consistent layout with minimal custom code.
        </p>
      </section>
    </main>
  );
}

export default AboutPage;

